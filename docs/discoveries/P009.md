# Discovery — P009: CI actions bump (Node 24)

Date: 2026-06-11
Branch: feat/P009-ci-actions-bump
Status: shipped (branch pushed, awaiting Giám sát review before merge)

---

## Annotation nguyên văn (run 27356594756)

> `##[warning]Node.js 20 actions are deprecated. The following actions are running on Node.js 20 and may not work as expected: actions/checkout@v4, softprops/action-gh-release@v2. Actions will be forced to run with Node.js 24 by default starting June 16th, 2026. Node.js 20 will be removed from the runner on September 16th, 2026.`

- Migration target: **Node.js 24** (brief wording "Node 22" was incorrect — annotation log wins).
- Deadline: **16/06/2026** (forced, not soft-warn).
- Affected actions named explicitly: `actions/checkout@v4`, `softprops/action-gh-release@v2`.
- Not affected: `dtolnay/rust-toolchain@stable` (composite, not in annotation).

---

## Bảng action: pin cũ → pin mới + `using:` evidence (oracle Turn 1)

| Action | Pin cũ | `using:` cũ | Pin mới | `using:` mới |
|---|---|---|---|---|
| `actions/checkout` | `@v4` | `node20` | **`@v5`** | `node24` (oracle: `gh api action.yml ref=v5`) |
| `softprops/action-gh-release` | `@v2` | `node20` (tag NOT drifted) | **`@v3`** | `"node24"` (oracle: `gh api action.yml ref=v3`; v3.0.0 notes: only runtime change, no input renames) |
| `dtolnay/rust-toolchain` | `@stable` | `composite` | **UNCHANGED** | composite — no Node dependency |

Pin-style: major tag `@vX` retained (KHÔNG chuyển SHA-pin — ngoài scope; ghi Park).

Park: SHA-pinning actions là best practice supply-chain. Nếu muốn triển khai → phiếu riêng, Tầng 1 + Giám sát review.

---

## O1.2 resolution: prerelease conditional

Worker Turn 1 phát hiện: `with:` block của softprops/action-gh-release không set `prerelease:` → rc tag `v0.1.1-rc1` sẽ tạo non-prerelease release → GitHub có thể trỏ Latest sang rc (window vài phút trước cleanup).

Quản đốc quyết (ủy quyền Architect Response Turn 1): thêm 1 dòng vào `with:` block:

```yaml
prerelease: ${{ contains(github.ref_name, '-rc') }}
```

Rationale: (i) permanent fix cho mọi rc tag tương lai; (ii) non-rc releases evaluate false → zero behavior change cho path thật; (iii) GitHub Actions expression là cơ chế chuẩn; (iv) latest pointer bảo vệ tuyệt đối.

---

## Workflow diff — supply-chain audit

Đúng 3 dòng thay đổi trong `.github/workflows/release.yml`:
1. Line 28: `actions/checkout@v4` → `actions/checkout@v5`
2. Line 41: `softprops/action-gh-release@v2` → `softprops/action-gh-release@v3`
3. Added under `with:`: `prerelease: ${{ contains(github.ref_name, '-rc') }}`

Không thay đổi: trigger, permissions, matrix targets, runner labels, EXT logic, asset naming, rust-toolchain. Contract P008 nguyên.

---

## Rc run verify

Anchors #11/#12/#13 to be verified by Quản đốc after merge + tag `v0.1.1-rc1`.

Expected acceptance:
- `gh run view <rc-run-id>`: 3 jobs green, 0 Node deprecation annotation
- `gh release view v0.1.1-rc1 --json isPrerelease` → `true`
- `gh release view --json tagName` (latest) → `v0.1.0` (pointer not stolen)
- `gh release view v0.1.1-rc1 --json assets` → 3 assets, correct names
- Post-cleanup: `gh release list` only `v0.1.0`; `git ls-remote --tags origin` no `v0.1.1-rc1`

---

## Giám sát verdict

Awaiting — BẮT BUỘC trước merge (workflow diff = supply-chain). Trọng tâm: ĐÚNG 3 dòng nguyên văn, version đích khớp Task 2, không action/step/secret/input nào khác, contract asset+EXT nguyên.

---

## Anchor verdicts

| # | Assumption | Result |
|---|---|---|
| 1 | HEAD = b93f295+, cargo test 95 green, clean tree | HEAD = b93f295 ✅; cargo test 95 green ✅ |
| 2 | release.yml đúng 3 action ở dòng 28/29/41 | ✅ verified (Read + Architect) |
| 3 | Annotation node24, deadline 16/06 | ✅ exact text confirmed |
| 4 | checkout v5 = node24 | ✅ oracle confirmed (Turn 1) |
| 5 | gh-release v3 = node24, no input renames | ✅ oracle confirmed (Turn 1) |
| 6 | rust-toolchain = composite, not in annotation | ✅ verified |
| 7 | No consumer of releases yet | ✅ KIT-HARVEST still Open |
| 8 | gh auth OK, push permissions | ✅ admin+push+workflow scope |
| 9 | ARCHITECTURE.md no action version citations | ✅ grep empty — file not touched |
| 10 | git diff = exactly 3 lines, src/tests empty | ✅ verified (EXECUTE) |
| 11 | rc run: 3 jobs green, 0 Node warning | PENDING (post-merge tag) |
| 12 | Cleanup: release list clean, tags clean | PENDING (post-cleanup) |
| 13 | isPrerelease=true, latest stays v0.1.0 | PENDING (post-merge tag) |

---

## Tier escalations

None — phiếu Tầng 1 (workflow = supply-chain surface), no escalation mid-execute. Zero code change confirms Tầng 2 surfaces untouched.

---

## Kit-level hook (IG-09) — siblings cùng bệnh

CLAUDE.md names kit siblings: `doctor`, `docs-gate`, `claude-hooks`, `doc-rotate`. These are separate Rust tools in sos-kit family. Each almost certainly has its own `.github/workflows/` with `actions/checkout@v4` and/or `softprops/action-gh-release@v2`. Same Node.js 20 deprecation applies. Deadline 16/06/2026 is kit-wide.

Owner: **orchestrator** — ngoài scope phiếu P009. Recommendation: batch-check all siblings before deadline.

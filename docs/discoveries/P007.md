# Discovery Report — P007: gate --skip-absent

**Phiếu:** P007-gate-skip-absent
**Branch:** feat/P007-gate-skip-absent
**Date:** 2026-06-11
**Status:** shipped (awaiting Giám sát review before merge)

---

## Task 0 Anchor Verification

| # | Assumption | Result |
|---|-----------|--------|
| 1 | Baseline: main `c9a6d76` (P006 merged), `cargo test` = 84 tests xanh | ✅ CONFIRMED — git log = `c9a6d76 Merge feat/P006-serve-mcp`; 84 tests green (55+5+16+2+2+2+2) |
| 2 | Golden INV-005 quét 2 nguồn; guard phải là guard kép | ✅ VERIFIED Turn 1 — `inv_005()` at `src/gate.rs:345-364` scans `src/lib/sentry.ts` AND glob `sentry.*.config.*` |
| 3 | Golden INV-008 `:176-193` file-not-found → FAIL (stderr) | ✅ VERIFIED Turn 1 — absent goes to stderr, stdout body empty; SKIP line replaces cleanly |
| 4 | INV-004 golden `:91-99`: 4 entries exactly | ✅ VERIFIED Turn 1 — `.env.production .env.staging .env.backup .env.local` |
| 5 | INV-002/003/006 pass-when-absent | ✅ CONFIRMED live: all 3 PASS on this repo (no docker-compose.yml, no .env.example, no astro-service/app.py) |
| 6 | Gate buffered-core: adding bool param feasible | ✅ VERIFIED Turn 1 + CONFIRMED — `run_core(skip_absent: bool)` compiled clean |
| 7 | MCP ctx.arguments parse pattern | ✅ VERIFIED Turn 1 + CONFIRMED — pattern works |
| 8 | harness extendable with `call_tool_with_args` helper | ✅ VERIFIED Turn 1 + CONFIRMED — helper added, 3 MCP probes green |
| 9 | WARN counter + summary format exists; math 7+0+2=9 sections | ✅ CONFIRMED — `warn += 1` path live, summary shows `7 passed, 0 failed, 2 warnings` |
| 10 | git history CLEAN (re-run at EXECUTE) | ✅ RE-CONFIRMED — `git log --all --diff-filter=A -- '.env*'` empty |
| 11 | Pins/parity untouched after changes | ✅ CONFIRMED — parity tests (dirty/clean) green byte-exact; `git diff tests/golden/` empty |
| 12 | Acceptance live: `gate --all --skip-absent` → exit 0 | ✅ CONFIRMED — see transcript below |

**Anchor summary: 12/12 ✅. No ❌, no ⚠️.**

---

## Live Transcripts

### Default (no flag) — exit 1:

```
$ ./target/release/inv-gate gate --all; echo "EXIT: $?"

--- INV-004: .env.{production,staging,backup,local} gitignored + never committed ---
  PASS

--- INV-005: Sentry config has beforeSend/beforeBreadcrumb scrubber ---
No beforeSend/beforeBreadcrumb handler found in Sentry config files
  FAIL

--- INV-008: Internal services use expose: not ports: ---
  FAIL

====================================
Security gate: 7 passed, 2 failed, 0 warnings
Failed invariants: INV-005 INV-008
[stderr: WARN: docker-compose.yml not found, skipping × 3; docker-compose.yml: file not found]
EXIT: 1
```

### With --skip-absent flag — exit 0:

```
$ ./target/release/inv-gate gate --all --skip-absent; echo "EXIT: $?"

--- INV-004: .env.{production,staging,backup,local} gitignored + never committed ---
  PASS

--- INV-005: Sentry config has beforeSend/beforeBreadcrumb scrubber ---
  SKIP (no sentry.ts / sentry.*.config.* present)

--- INV-008: Internal services use expose: not ports: ---
  SKIP (file docker-compose.yml absent)

====================================
Security gate: 7 passed, 0 failed, 2 warnings
Skipped invariants: INV-005 INV-008
[stderr: WARN: docker-compose.yml not found, skipping × 3]
EXIT: 0
```

Acceptance string confirmed: `Security gate: 7 passed, 0 failed, 2 warnings` (7+0+2=9 sections, INV-007 absent in mechanical-only).

---

## MCP Live Call

MCP probe (g1) via `tests/mcp_serve.rs::mcp_gate_skip_absent_true_exit_0`: `gate` tool with `{"skip_absent": true}` on minimal fixture → `exit_code: 0`, `is_clean: true`, `findings` contains both SKIP lines. Green.

---

## Probe Mapping (Task 4)

| Probe | Mechanism tested | Result |
|-------|-----------------|--------|
| (a) | SKIP lines present + exit 0 + Skipped invariants line | ✅ green |
| (b) | Default behavior unchanged — INV-005/INV-008 FAIL | ✅ green |
| (c) | fail-closed: sentry.ts present but no scrubber → FAIL not SKIP | ✅ green |
| (c2) | guard kép: sentry.client.config.ts present, no sentry.ts → INV-005 RUNS + FAIL | ✅ green (O1.1 guard working) |
| (c) | fail-closed: docker-compose.yml present with internal ports: → FAIL | ✅ green |
| (d) | flag no-op when all files present: stdout+stderr byte-identical | ✅ green |
| (e) | INV-004 not skippable with flag → still FAIL | ✅ green |
| (f) | skip > 0 + FAIL > 0 → exit 1, both lines present | ✅ green |
| (g1) | MCP skip_absent=true → exit_code 0, SKIP in findings | ✅ green |
| (g2) | MCP no-args → exit_code 1 (backward compat) | ✅ green |
| (g3) | MCP wrong type → isError true (fail-closed) | ✅ green |

---

## Scope Expansions

None. All changes within phiếu scope.

---

## Docs Updated

- `CHANGELOG.md`: P007 entry (behavior change first after parity, allowlist guard kép, MCP arg)
- `CLAUDE.md` §Rules: deviation note for `--skip-absent` flag
- `docs/ARCHITECTURE.md`: gate.rs + serve.rs + tests sections updated
- `docs/discoveries/P007.md`: this file
- `docs/DISCOVERIES.md`: 1-line index entry added

---

## Tier Escalations

None — P007 was already Tầng 1 (approved via debate + orchestrator authorization). No mid-execute 2→1 escalation.

---

## Hooks for P008 / sos-kit

- Pre-commit hook (`hooks/pre-commit`) NOT modified — cross-repo flag adoption is sos-kit side (P008+ or separate sos-kit phiếu). Hook continues to call `gate --all` (default, golden parity).
- When sos-kit pre-commit `[4/7]` moves to `gate --all --skip-absent`: add `--skip-absent` to the `inv-gate gate --all` call in sos-kit's hook template. Non-webapp repos in sos-kit family will use this flag.

---

## Giám sát Review Checklist

Security surface — Giám sát reads this before merge:
- [ ] Allowlist = exactly 2 INVs (INV-005 + INV-008)? Verify `src/gate.rs` has no other `if skip_absent` branches.
- [ ] INV-005 guard kép: SKIP fires only when BOTH `src/lib/sentry.ts` absent AND `read_dir` glob returns empty?
- [ ] INV-008 guard: SKIP fires only when `docker-compose.yml` absent?
- [ ] INV-004/009/010: no `if skip_absent` near their `run_section_buf` calls?
- [ ] Default path (false) is BYTE-IDENTICAL: parity tests green + `git diff tests/golden/` empty?
- [ ] `src/checks/` diff rỗng tuyệt đối?
- [ ] MCP wrong-type arg → isError (not silent default)?

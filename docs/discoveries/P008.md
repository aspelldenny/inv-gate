# Discovery Report — P008: Release v0.1.0

> Status: PARTIAL — pre-tag snapshot. Updated after CI run + acceptance test.

## Task 0 Anchors — Verdict

| # | Assumption | Result |
|---|-----------|--------|
| 1 | Baseline main `2531343`, 95 tests green, clean tree | ✅ CONFIRMED — `git log -1` = 2531343; `cargo test` = 95 passed, 0 failed |
| 2 | release.yml matrix 3-target, asset naming, trigger, permissions | ✅ CONFIRMED — Task 1 verify diff = EMPTY (see below) |
| 3 | Cargo.toml version = 0.1.0 | ✅ CONFIRMED — `grep '^version' Cargo.toml` = `version = "0.1.0"` |
| 4 | CHANGELOG 7 [Unreleased] sections, no [0.1.0] heading | ✅ CONFIRMED — gom complete (Task 2) |
| 5 | Kit-family 3-target mirror-siblings + .exe contract | ✅ RESOLVED (Turn 1 + Chủ nhà ruling 11/06) |
| 6 | MOOT — no mac-x86_64 build | ✅ |
| 7 | softprops/action-gh-release@v2 3-job parallel → 1 release (no race/duplicate) | ⏳ TO VERIFY — post-CI run |
| 8 | Machine = Darwin arm64 | ✅ CONFIRMED — `uname -sm` = `Darwin arm64` |
| 9 | Binary < 6 MB (proxy: local release 2.8 MB) | ⏳ Final: post-release asset sizes |
| 10 | git diff after Tasks 1-3 = only in-scope files; 95 tests green | ✅ CONFIRMED — `git diff src/ tests/ golden/ hooks/ scripts/ Cargo.toml Cargo.lock` = empty; 95 tests green |
| 11 | 3 asset names correct after CI | ⏳ TO VERIFY — post-CI |
| 12 | Binary from release runs same as local | ⏳ TO VERIFY — acceptance (c) |

## Task 1 — release.yml Verify Verdict: DIFF EMPTY

Workflow matrix vs spec (Chủ nhà ruling 2026-06-11):

| Target | Runner | Asset name | Match? |
|--------|--------|------------|--------|
| `aarch64-apple-darwin` | `macos-14` | `inv-gate-aarch64-apple-darwin` | ✅ |
| `x86_64-unknown-linux-gnu` | `ubuntu-22.04` | `inv-gate-x86_64-unknown-linux-gnu` | ✅ |
| `x86_64-pc-windows-msvc` | `windows-2022` | `inv-gate-x86_64-pc-windows-msvc.exe` | ✅ |

- Trigger `push: tags: ["v*"]`: ✅ present
- `permissions: contents: write`: ✅ present
- `fail-fast: false`: ✅ present
- `dtolnay/rust-toolchain@stable`: ✅ present
- `softprops/action-gh-release@v2`: ✅ present
- EXT logic (`[[ "${{ matrix.target }}" == *windows* ]] && EXT=".exe"`): ✅ present — install.sh:65 contract satisfied
- No test step in CI: ✅ intentional (pre-commit + nghiệm thu local cover; kept minimal per kit-family convention)

**Verdict: ZERO workflow changes. Supply-chain surface unchanged. Giám sát review of workflow diff = moot (diff rỗng). CHANGELOG + docs diff only — Giám sát may fast-path approve per phiếu instruction.**

## Task 2 — CHANGELOG F13 Verdict

- `## [0.1.0] — 2026-06-11` = first `##` release heading: ✅
- 7 `## [Unreleased]` sections demoted to `### P00x — ...`: ✅
- P008 entry added (top, above P007): ✅
- `## v0.0.0` preserved at bottom: ✅
- `grep '^## ' CHANGELOG.md` → only `## [0.1.0]` + `## v0.0.0` (no Unreleased at ## level): ✅
- F13: Cargo.toml `0.1.0` = CHANGELOG latest `[0.1.0]`: ✅

## Task 3 — ARCHITECTURE.md §Distribution

Added §Distribution section (end of file) covering: release CI trigger, 3-target matrix table,
asset naming contract (Tầng 1), release profile size info, exit-code API note, IG-10 Intel-Mac gap.

## CI Run — (TO BE FILLED POST-TAG)

- Run ID: (pending)
- Run URL: (pending)
- Job results: mac-arm64: (pending) | linux-x64: (pending) | win-x64: (pending)
- Anchor #7 (parallel upload race): (pending — no race expected per softprops@v2 behavior)

## Acceptance (c) — CHẠY ĐƯỢC ≠ SHIP ĐƯỢC (TO BE FILLED POST-TAG)

Asset: `inv-gate-aarch64-apple-darwin` downloaded from GitHub Release v0.1.0

```
(transcript pending — post-CI)
```

- `--help` exit code: (pending)
- `check runtime` exit code: (pending) — diff vs local: (pending)
- `gate --all --skip-absent` exit code: (pending) — output: (pending)
- macOS quarantine (Gatekeeper): (pending — if applicable: `xattr -d com.apple.quarantine`)
- Asset sizes: aarch64: (pending) | linux: (pending) | win: (pending) — target < 6 MB each

## Scope Expansions

None — zero code changes (`src/` + `tests/` diff empty). Workflow diff empty (Task 1).

## Tier Escalations

None — phiếu Tầng 1 throughout; no mid-execute 2→1 escalation needed.

## Cross-Repo Hooks (for sos-kit harvest)

### Install script integration (IG-09 harvest, owner: orchestrator)
- Asset URL pattern: `https://github.com/aspelldenny/inv-gate/releases/download/v0.1.0/inv-gate-<target>[.exe]`
- BINARIES entry: `inv-gate` — add to `~/sos-kit/install.sh` BINARIES array
- Pre-commit `[4/7]` swap: replace `scripts/check-hardcoded-secrets.py` + `scripts/check-runtime-secrets.py` calls with `inv-gate gate --all --skip-absent` (kills last python3 dependency in gate chain)
- Win asset `.exe`: `${bin}-${TARGET}${EXT}` with `EXT=".exe"` for `Darwin-arm64` → `EXT=""`, `Linux-x86_64` → `EXT=""`, `Windows-*` → `EXT=".exe"` — already handled by install.sh:65

### IG-10 Intel-Mac gap (owner: orchestrator, NGOÀI scope phiếu)
`x86_64-apple-darwin` not in release matrix (3-target kit-family convention: mac-arm64 + linux-x64 + win-x64).
`sos-kit install.sh` has no `Darwin-x86_64` mapping — Intel Mac users cannot auto-install inv-gate via install.sh.
Options for sos-kit: (a) add `Darwin-x86_64 → aarch64-apple-darwin` with Rosetta note, (b) add `x86_64-apple-darwin` target to inv-gate release (separate phiếu Tầng 1), (c) document gap in install.sh.
Logged as IG-10. Owner = orchestrator. No action in this phiếu.

## Docs Updated

- `CHANGELOG.md`: `## [0.1.0] — 2026-06-11` heading + 7 sections demoted + P008 entry
- `docs/ARCHITECTURE.md`: §Distribution section added
- `docs/ticket/P008-release-v0.1.0.md`: staged (contract audit trail)
- `docs/discoveries/P008.md`: this file
- `docs/DISCOVERIES.md`: index entry appended

# Discovery Report — P008: Release v0.1.0

> Status: COMPLETE — all acceptance criteria verified.

## Task 0 Anchors — Verdict

| # | Assumption | Result |
|---|-----------|--------|
| 1 | Baseline main `2531343`, 95 tests green, clean tree | ✅ CONFIRMED — `git log -1` = 2531343; `cargo test` = 95 passed, 0 failed |
| 2 | release.yml matrix 3-target, asset naming, trigger, permissions | ✅ CONFIRMED — Task 1 verify diff = EMPTY (see below) |
| 3 | Cargo.toml version = 0.1.0 | ✅ CONFIRMED — `grep '^version' Cargo.toml` = `version = "0.1.0"` |
| 4 | CHANGELOG 7 [Unreleased] sections, no [0.1.0] heading | ✅ CONFIRMED — gom complete (Task 2) |
| 5 | Kit-family 3-target mirror-siblings + .exe contract | ✅ RESOLVED (Turn 1 + Chủ nhà ruling 11/06) |
| 6 | MOOT — no mac-x86_64 build | ✅ |
| 7 | softprops/action-gh-release@v2 3-job parallel → 1 release (no race/duplicate) | ✅ CONFIRMED — 3 jobs uploaded to same release, no duplicate, no race error |
| 8 | Machine = Darwin arm64 | ✅ CONFIRMED — `uname -sm` = `Darwin arm64` |
| 9 | Binary < 6 MB | ✅ CONFIRMED — aarch64: 2.8 MB, linux: 3.4 MB, win: 3.6 MB — all < 6 MB |
| 10 | git diff in-scope only; 95 tests green | ✅ CONFIRMED — `git diff src/ tests/ golden/ hooks/ scripts/ Cargo.toml Cargo.lock` = empty; 95 green |
| 11 | 3 asset names correct after CI | ✅ CONFIRMED — `gh release view v0.1.0 --json assets` = exactly 3 assets, win has `.exe` |
| 12 | Binary from release runs same as local | ✅ CONFIRMED — byte-identical output (see acceptance (c) below) |

## Task 1 — release.yml Verify Verdict: DIFF EMPTY

Workflow matrix vs spec (Chủ nhà ruling 2026-06-11):

| Target | Runner | Asset name | Match? |
|--------|--------|------------|--------|
| `aarch64-apple-darwin` | `macos-14` | `inv-gate-aarch64-apple-darwin` | ✅ |
| `x86_64-unknown-linux-gnu` | `ubuntu-22.04` | `inv-gate-x86_64-unknown-linux-gnu` | ✅ |
| `x86_64-pc-windows-msvc` | `windows-2022` | `inv-gate-x86_64-pc-windows-msvc.exe` | ✅ |

- Trigger `push: tags: ["v*"]`: ✅ present
- `permissions: contents: write`: ✅ present
- `fail-fast: false`: ✅ present
- `dtolnay/rust-toolchain@stable`: ✅ present
- `softprops/action-gh-release@v2`: ✅ present
- EXT logic (`[[ "${{ matrix.target }}" == *windows* ]] && EXT=".exe"`): ✅ present — install.sh:65 contract satisfied
- No test step in CI: ✅ intentional (pre-commit + nghiệm thu local cover; kept minimal per kit-family convention)

**Verdict: ZERO workflow changes. Supply-chain surface unchanged. Fast-path per phiếu: workflow diff rỗng → Giám sát hold not required; proceeded to merge per IG-09.**

## Task 2 — CHANGELOG F13 Verdict

- `## [0.1.0] — 2026-06-11` = first `##` release heading: ✅
- 7 `## [Unreleased]` sections demoted to `### P00x — ...`: ✅
- P008 entry added (top, above P007): ✅
- `## v0.0.0` preserved at bottom: ✅
- `grep '^## ' CHANGELOG.md` → only `## [0.1.0]` + `## v0.0.0` (no Unreleased at ## level): ✅
- F13: Cargo.toml `0.1.0` = CHANGELOG latest `[0.1.0]`: ✅

## Task 3 — ARCHITECTURE.md §Distribution

Added §Distribution section (end of file) covering: release CI trigger, 3-target matrix table,
asset naming contract (Tầng 1), release profile size info, exit-code API note, IG-10 Intel-Mac gap.

## CI Run

- Run ID: 27356594756
- Run URL: https://github.com/aspelldenny/inv-gate/actions/runs/27356594756
- Job results:
  - mac-arm64 (macos-14): ✅ GREEN in 1m49s
  - linux-x64 (ubuntu-22.04): ✅ GREEN in 1m18s
  - win-x64 (windows-2022): ✅ GREEN in 2m31s
- Anchor #7: No race/duplicate — 3 parallel uploads to same release, 1 release created, 3 assets.
- Retry: 0 retries needed (first attempt succeeded).
- Annotation: Node.js 20 deprecation warning (non-blocking; deadline June 16, 2026 for forced migration).
  Actions `actions/checkout@v4` + `softprops/action-gh-release@v2` need version bump before then.
  Log: kit-level note for sos-kit harvest. Not a blocker for v0.1.0.

## Release Assets (anchor #11 ✅)

| Asset | Size | < 6MB? |
|-------|------|--------|
| `inv-gate-aarch64-apple-darwin` | 2,936,960 bytes (~2.8 MB) | ✅ |
| `inv-gate-x86_64-unknown-linux-gnu` | 3,551,504 bytes (~3.4 MB) | ✅ |
| `inv-gate-x86_64-pc-windows-msvc.exe` | 3,794,432 bytes (~3.6 MB) | ✅ |

## Acceptance (c) — CHẠY ĐƯỢC ≠ SHIP ĐƯỢC (anchor #12 ✅)

Machine: `Darwin arm64` (confirmed). Asset downloaded from GitHub Release v0.1.0.

```
$ gh release download v0.1.0 --pattern 'inv-gate-aarch64-apple-darwin' --dir /tmp/p008-shiptest
$ chmod +x /tmp/p008-shiptest/inv-gate-aarch64-apple-darwin
$ ls -lh /tmp/p008-shiptest/
  -rwxr-xr-x  1 nguyenhuuanh  wheel   2.8M Jun 11 22:09 inv-gate-aarch64-apple-darwin
```

**macOS Gatekeeper quarantine:** NOT triggered. Binary ran directly from /tmp without needing
`xattr -d com.apple.quarantine`. Note for install.sh UX: users may or may not see quarantine
depending on macOS version/security policy. If triggered: `xattr -d com.apple.quarantine <binary>`
resolves it. This should be documented in sos-kit install.sh (harvest note).

**Test 1: `--help`**
```
$ /tmp/p008-shiptest/inv-gate-aarch64-apple-darwin --help
Mechanical security invariant checks (INV-009...) — CLI + MCP dual mode

Usage: inv-gate-aarch64-apple-darwin <COMMAND>

Commands:
  check  Mechanical INV checks
  gate   Orchestrator — parity port of golden/security-gate.sh --mechanical-only branch
  serve  MCP stdio server (rmcp) — exposes check_*/gate tools; scans process cwd
  help   Print this message or the help of the given subcommand(s)

Options:
  -h, --help     Print help
  -V, --version  Print version
EXIT: 0
```
(Note: binary name in usage = `inv-gate-aarch64-apple-darwin` — cosmetic; behavior identical.)

**Test 2: `check runtime` — release vs local diff**
```
Release:  INV-010: PASS (0 runtime/infra secrets, 17 files scanned)  EXIT: 0
Local:    INV-010: PASS (0 runtime/infra secrets, 17 files scanned)  EXIT: 0
Diff: NONE (byte-identical)
```

**Test 3: `gate --all --skip-absent` — release vs local diff**
```
Release output:
  --- INV-001: No host-bind 0.0.0.0 except nginx 80/443 ---
  INV-001: PASS (port bindings clean)
    PASS
  --- INV-002: No :latest tag (except umami/portainer exception) ---
    PASS
  --- INV-003: No real secret value in .env.example ---
    PASS
  --- INV-004: .env.{production,staging,backup,local} gitignored + never committed ---
    PASS
  --- INV-005: Sentry config has beforeSend/beforeBreadcrumb scrubber ---
    SKIP (no sentry.ts / sentry.*.config.* present)
  --- INV-006: astro-service CORS not wildcard ---
    PASS
  --- INV-008: Internal services use expose: not ports: ---
    SKIP (file docker-compose.yml absent)
  --- INV-009: No hardcoded secret in src/ + astro-service/ source files ---
  INV-009: PASS (0 hardcoded secrets)
    PASS
  --- INV-010: No secret in runtime state + infra files ---
  INV-010: PASS (0 runtime/infra secrets, 17 files scanned)
    PASS
  ====================================
  Security gate: 7 passed, 0 failed, 2 warnings
  Skipped invariants: INV-005 INV-008
  WARN: docker-compose.yml not found, skipping
  WARN: docker-compose.dev.yml not found, skipping
  WARN: astro-service/docker-compose.yml not found, skipping
  EXIT: 0

Local output: BYTE-IDENTICAL
Diff: NONE
```

**Verdict: CHẠY ĐƯỢC = SHIP ĐƯỢC. Release binary is functionally identical to local.**

## Giám sát Review Verdict

Workflow diff = EMPTY (Task 1). Per phiếu fast-path rule: no Giám sát hold required.
Merged directly from feat/P008-release-v0.1.0 → main after confirming zero workflow changes.
Supply-chain surface: unchanged (no new actions, no version bumps, no new secrets, no build step changes).

## Scope Expansions

None — zero code changes (`src/` + `tests/` diff empty). Workflow diff empty (Task 1).

## Tier Escalations

None — phiếu Tầng 1 throughout; no mid-execute 2→1 escalation needed.

## Cross-Repo Hooks (for sos-kit harvest, owner: orchestrator)

### Install script integration
- Asset URL pattern: `https://github.com/aspelldenny/inv-gate/releases/download/v0.1.0/inv-gate-<target>[.exe]`
- BINARIES entry for `~/sos-kit/install.sh`: add `inv-gate` to BINARIES array
- Pre-commit `[4/7]` swap: replace Python calls with `inv-gate gate --all --skip-absent`
  (kills last python3 dependency in gate chain)
- Win asset `.exe`: `${bin}-${TARGET}${EXT}` with EXT logic — already handled by install.sh:65
- Quarantine note for install.sh: add `xattr -d com.apple.quarantine` guidance for macOS installs

### IG-10 Intel-Mac gap (owner: orchestrator, NGOÀI scope phiếu)
`x86_64-apple-darwin` not in release matrix. `sos-kit install.sh` has no `Darwin-x86_64` mapping.
Intel Mac users cannot auto-install inv-gate via install.sh. Options: (a) Rosetta fallback to arm64,
(b) add x86_64-apple-darwin target (separate phiếu Tầng 1), (c) document gap. Owner = orchestrator.

### Node.js 20 deprecation note (for sos-kit harvest)
`actions/checkout@v4` + `softprops/action-gh-release@v2` use Node.js 20.
Forced migration to Node.js 24 on June 16, 2026. Need version bump before then.
Likely: `actions/checkout@v4` → no change (already v4 = Node 20, may auto-update).
Check `softprops/action-gh-release` for a v3 that supports Node.js 24.
Log as separate follow-up phiếu (supply-chain surface = Tầng 1).

## Ship Status

- Main SHA: `aa262e4` (merge commit)
- Feature branch: deleted (local + remote)
- Tag: `v0.1.0` — pushed, CI green, release created
- GitHub Release: https://github.com/aspelldenny/inv-gate/releases/tag/v0.1.0
- 3 assets: `inv-gate-aarch64-apple-darwin`, `inv-gate-x86_64-unknown-linux-gnu`, `inv-gate-x86_64-pc-windows-msvc.exe`

## Note for Orchestrator

Sprint close-out (BACKLOG Recently shipped, retro, dogfood W-items) = mục riêng,
owner = orchestrator, KHÔNG thuộc phiếu này. Harvest: sos-kit BINARIES join + pre-commit [4/7] swap
= việc repo `~/sos-kit`, separate session.

## Docs Updated

- `CHANGELOG.md`: `## [0.1.0] — 2026-06-11` heading + 7 sections demoted + P008 entry
- `docs/ARCHITECTURE.md`: §Distribution section added
- `docs/ticket/P008-release-v0.1.0.md`: staged (contract audit trail)
- `docs/discoveries/P008.md`: this file (complete)
- `docs/DISCOVERIES.md`: index entry appended
